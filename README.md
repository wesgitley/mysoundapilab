# Sound Lab API

Simple Node.js Express API for sound experiments.

## Setup

1. `npm install`
2. Copy `.env.example` to `.env` and set PORT.
3. `npm run dev` for development.

## Endpoints

- GET / - Health check
- GET /api/sounds - List sounds
